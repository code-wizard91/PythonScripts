#Experimenting with Tuples

#Tuples are immutable and cannot be changed, so they are used to present things that shouldn't be changed

#can also contain different types of elements e.g strings,integers,booleans

tTuple = (1,'mizan',3) #this is how we create a tuple

print(tTuple[2])